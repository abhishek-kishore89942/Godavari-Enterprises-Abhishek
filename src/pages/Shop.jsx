import {useMemo,useState} from 'react'
import {useSearchParams} from 'react-router-dom'
import {FiFilter,FiSearch,FiX} from 'react-icons/fi'
import {products} from '../data'
import {PageHero,ProductCard} from '../components/UI'
export default function Shop(){const [params]=useSearchParams();const [search,setSearch]=useState('');const [category,setCategory]=useState(params.get('category')||'All');const [size,setSize]=useState('All');const [max,setMax]=useState(8000);const [sort,setSort]=useState('latest');const [filters,setFilters]=useState(false);const list=useMemo(()=>products.filter(p=>(category==='All'||p.category===category)&&(size==='All'||p.sizes.includes(size))&&p.price<=max&&p.title.toLowerCase().includes(search.toLowerCase())).sort((a,b)=>sort==='low'?a.price-b.price:sort==='high'?b.price-a.price:b.id.localeCompare(a.id)),[search,category,size,max,sort]);return <><PageHero eyebrow="Spring / Summer 2026" title="The Collection" copy="Enduring silhouettes, natural textures, and a modern Indian point of view."/><div className="shop-toolbar wrap"><label className="search-box"><FiSearch/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search the collection"/></label><button className="filter-toggle" onClick={()=>setFilters(!filters)}><FiFilter/> Filters</button><span>{list.length} pieces</span><select value={sort} onChange={e=>setSort(e.target.value)} aria-label="Sort"><option value="latest">Sort: Latest</option><option value="low">Price: Low to high</option><option value="high">Price: High to low</option></select></div><div className="shop-layout wrap"><aside className={filters?'show':''}><button className="filter-close" onClick={()=>setFilters(false)}><FiX/></button><div className="filter"><h4>Category</h4>{['All','Women','Men'].map(x=><button className={category===x?'selected':''} onClick={()=>setCategory(x)} key={x}>{x}</button>)}</div><div className="filter"><h4>Size</h4><div className="size-filter">{['All','XS','S','M','L','XL','XXL','Free'].map(x=><button className={size===x?'selected':''} onClick={()=>setSize(x)} key={x}>{x}</button>)}</div></div><div className="filter"><h4>Price up to ₹{max.toLocaleString('en-IN')}</h4><input type="range" min="2000" max="8000" step="500" value={max} onChange={e=>setMax(Number(e.target.value))}/></div></aside><div>{list.length?<div className="product-grid shop-grid">{list.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty"><h3>No pieces found</h3><p>Try widening your filters.</p></div>}<div className="pagination"><button className="active">1</button><button>2</button><button>Next →</button></div></div></div></>}
