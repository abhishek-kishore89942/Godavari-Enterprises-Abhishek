import {motion} from 'framer-motion'
import {Link} from 'react-router-dom'
import {FiArrowUpRight,FiHeart,FiStar} from 'react-icons/fi'
import {useApp} from '../context/AppContext'
export const money=n=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n)
export function SectionTitle({eyebrow,title,copy,action}){return <motion.div className="section-head" initial={{opacity:0,y:28}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-80px'}} transition={{duration:.7,ease:[.22,1,.36,1]}}><div><span className="eyebrow">{eyebrow}</span><h2>{title}</h2>{copy&&<p>{copy}</p>}</div>{action&&<Link to={action}>View all <FiArrowUpRight/></Link>}</motion.div>}
export function ProductCard({product}){const {wishlist,toggleWish}=useApp();return <motion.article layout className="product-card" initial={{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-60px'}} transition={{duration:.55,ease:[.22,1,.36,1]}} whileHover={{y:-7}}><Link to={`/product/${product.id}`} className="product-image"><img src={product.images[0]} alt={product.title} loading="lazy"/><span className="product-shine"/><span className="product-hover">Quick view <FiArrowUpRight/></span>{product.badge&&<span className="badge">{product.badge}</span>}</Link><button type="button" className={`wish ${wishlist.includes(product.id)?'active':''}`} onClick={()=>toggleWish(product.id)} aria-label="Add to wishlist"><FiHeart/></button><div className="product-copy"><div><p className="product-type">{product.type}</p><Link to={`/product/${product.id}`}>{product.title}</Link></div><strong>{money(product.price)}</strong></div><div className="rating"><FiStar/> {product.rating} <span>• {product.stock} in stock</span></div></motion.article>}
export function PageHero({eyebrow,title,copy}){return <motion.section className="page-hero wrap" initial={{opacity:0,y:28}} animate={{opacity:1,y:0}} transition={{duration:.75,ease:[.22,1,.36,1]}}><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{copy}</p></motion.section>}
